image: paritytech/ink-ci-linux

workspaceLocation: "./flipper"

tasks: 
  - command: cargo contract new flipper
  - command: cd ./flipper && cargo +nightly contract build

vscode:
  extensions:
    - matklad.rust-analyzer
    - bungcip.better-toml
